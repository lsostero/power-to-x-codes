the .m files are matlab files that simulates the proces of a power-to-methanol\ammonia chain. 
for some codes it is needed to import some data set to matlab. All the data must be imported as column vectors
